# docker-glassfish

A repo containing glassfish docker `Dockerfile` files which can be use to create docker images of specific glassfish versions.  
Currently supported images are for:
* Glassish v2ur2 with openjdk v1.7u151, see [Dockerfile](glassfish-v2ur2/Dockerfile) at `glassfish-v2ur2` folder
